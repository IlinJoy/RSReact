// import { redirect } from 'react-router';

// export const checkIsValidNumberToQuery = (value: string = '') => {
//   if (!/^[1-9][0-9]*$/.test(value)) {
//     throw redirect('/not-found');
//   }
// };

// export const redirectWithStoredQuery = (url: URL, queryFromStorage: string) => {
//   url.searchParams.set('query', queryFromStorage);
//   throw redirect(url.toString());
// };
